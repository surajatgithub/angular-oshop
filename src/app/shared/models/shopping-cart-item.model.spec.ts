import { ShoppingCartItem } from './shopping-cart-item.model';

describe('ShoppingCartItem', () => {
  it('should create an instance', () => {
    expect(new ShoppingCartItem()).toBeTruthy();
  });
});
